# installed postgresql

# created database and table
su -s /bin/bash postgres
psql

CREATE SCHEMA "check";

SET SEARCH_PATH TO "check";

CREATE TABLE log (
	log_id SERIAL PRIMARY KEY NOT NULL,
	text TEXT NOT NULL,
	date TIMESTAMP NOT NULL,
	CONSTRAINT text_length CHECK (CHAR_LENGTH(text) <= 50)
);

